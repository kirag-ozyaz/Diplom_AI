## 1. Запуск docx_to_md_images_1.py
X:\Учеба_УИИ\Итоговы_Проект\_Project\pythonProject\.venv\Scripts\python.exe X:\Учеба_УИИ\Итоговы_Проект\_Project\pythonProject\Create_md\docx_to_md_images_1.py "X:\Учеба_УИИ\Итоговы_Проект\Этап №2.  AI_ML  Сбор базы\Нормативная база\ПУЭ\DOCX\2.4.docx"
X:\Учеба_УИИ\Итоговы_Проект\_Project\pythonProject\.venv\Scripts\python.exe X:\Учеба_УИИ\Итоговы_Проект\_Project\pythonProject\Create_md\docx_to_md_images_1.py "X:\Учеба_УИИ\Итоговы_Проект\Этап №2.  AI_ML  Сбор базы\Нормативная база\ПУЭ\DOCX\2.5.docx" 

## 2. Запуск generate.py 
# Конвертация файлов в ./docs → ./md (4 параллельных задачи)
python generator.py -i ./docs -o ./md -j 4

# Рекурсивная обработка вложенных папок
python generator.py -i ./docs -o ./md -r -j 6



Скрипт поддерживает следующие параметры запуска:

---

### 📋 Полный список аргументов

| Аргумент | Краткая форма | Тип | Значение по умолчанию | Описание |
|----------|---------------|-----|------------------------|----------|
| `--input` | `-i` | путь | `.` (текущая папка) | Входная директория с `.docx`-файлами |
| `--output` | `-o` | путь | `./output` | Выходная директория для `.md`-файлов |
| `--jobs` | `-j` | целое число | `4` | Максимальное число параллельных конвертаций |
| `--recursive` | `-r` | флаг | отсутствует | Рекурсивный поиск во вложенных папках |
| `--help` | `-h` | флаг | — | Показать справку и выйти |

---

### 🔍 Детальное описание

#### `-i, --input ПУТЬ`
Входная директория, где находятся `.docx`-файлы.

```bash
# Примеры:
python generate.py -i ./documents          # относительный путь
python generate.py --input /home/user/docs # абсолютный путь
python generate.py                         # текущая директория (по умолчанию)
```

> ⚠️ Если папка не существует — скрипт завершится с ошибкой.

---

#### `-o, --output ПУТЬ`
Директория для сохранения конвертированных `.md`-файлов. Создаётся автоматически при необходимости.

```bash
# Примеры:
python generate.py -o ./markdown           # относительный путь
python generate.py --output /tmp/results   # абсолютный путь
python generate.py -i ./docs -o ./docs-md  # разные вход/выход
```

> 💡 Структура подпапок сохраняется относительно `--input`.  
> Пример: `./docs/2024/report.docx` → `./markdown/2024/report.md`

---

#### `-j, --jobs ЧИСЛО`
Ограничение параллелизма: сколько файлов обрабатывать одновременно.

```bash
# Примеры:
python generate.py -j 2    # для слабых ПК / маломощных серверов
python generate.py -j 8    # для многоядерных систем
python generate.py -j 1    # строго последовательная обработка
```

> 💡 Рекомендации:
> - `2–4` — оптимально для большинства случаев
> - `1` — для отладки (ошибки проще отследить)
> - `>8` — только при наличии мощного CPU и быстрого диска

---

#### `-r, --recursive`
Флаг для рекурсивного поиска `.docx` во всех вложенных подпапках.

```bash
# Без рекурсии (только корень):
python generate.py -i ./docs

# С рекурсией (все уровни вложенности):
python generate.py -i ./docs -r
```

> Пример структуры при использовании `-r`:
> ```
> ./docs/
> ├── report.docx
> └── 2024/
>     ├── january.docx
>     └── february.docx
> ```
> Результат в `./output/`:
> ```
> ./output/
> ├── report.md
> └── 2024/
>     ├── january.md
>     └── february.md
> ```

---

### 🧪 Примеры запуска

```bash
# 1. Минимальный запуск (текущая папка → ./output)
python generate.py

# 2. Конвертация с сохранением структуры папок
python generate.py -i ./source -o ./result -r -j 6

# 3. Только корневая папка, 2 параллельных задачи
python generate.py -i ./docs -o ./md -j 2

# 4. Полный путь с рекурсией
python generate.py --input /data/reports --output /data/markdown --recursive --jobs 4

# 5. Показать справку
python generate.py -h
```

---

### 🖨️ Вывод справки (`-h`)

```text
usage: generate.py [-h] [-i INPUT] [-o OUTPUT] [-j JOBS] [-r]

Асинхронная конвертация DOCX → Markdown

options:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Входная папка с DOCX-файлами (по умолчанию: текущая директория)
  -o OUTPUT, --output OUTPUT
                        Выходная папка для Markdown (по умолчанию: ./output)
  -j JOBS, --jobs JOBS  Макс. параллельных конвертаций (по умолчанию: 4)
  -r, --recursive       Рекурсивный поиск во вложенных папках

Пример: python generate.py -i ./docs -o ./md -j 6
```

---

### ⚠️ Важные замечания

1. **Регистр расширения**  
   Скрипт обрабатывает `.docx`, `.DOCX`, `.DocX` — регистронезависимо.

2. **Перезапись файлов**  
   Существующие `.md`-файлы в выходной папке будут перезаписаны без предупреждения.

3. **Ошибки конвертации**  
   Сбой при обработке одного файла **не останавливает** весь процесс — остальные файлы продолжат конвертироваться.

4. **Требования к функции конвертации**  
   Функция `docx_to_md_with_images(docx_path: str) -> str` должна:
   - Принимать путь к `.docx` как строку
   - Возвращать строку с валидным Markdown
   - Выбрасывать исключения при ошибках (скрипт их перехватит)